<div class="container">
    <h1>POSTS</h1>
    <div class="post-info">
        {{#each posts as |post| }}
        {{> post-info post}}


    </div>
    {{/each}}
</div>

{{User.username}} {{format_date createdAt}}