<div class="container">
    <h1>POSTS</h1>
    {{#each posts}}
    <div class="post-info">
        <h2>{{ title }}</h2>
        <p>{{ content }}</p>

    </div>
    {{/each}}
</div>

{{!-- {{User.username}} {{format_date createdAt}} --}}